@extends('layouts.app')
@section('titulo', 'Confirmar correo')
@section('pagina_ingreso', true)
@section('contenido')
<div class="login-page"><section class="login-visual" aria-label="Confirmación"><div class="login-visual__content"><a href="{{ url('/') }}" class="brand brand--light"><span class="brand__mark">EC</span><span>Estado de Cuenta</span></a><div class="login-visual__message"><span class="eyebrow">Verificación</span><h1>Confirma tu correo para proteger tu cuenta.</h1></div></div></section><main class="login-panel"><div class="login-card"><div class="login-card__heading"><span class="login-card__icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M4 6h16v12H4z"/><path d="m4 7 8 6 8-6"/></svg></span><div><p class="eyebrow text-primary-600">Último paso</p><h2>Revisa tu correo</h2></div></div><p class="login-card__description">Ingresa el código de 6 dígitos enviado a tu correo. Vence en 10 minutos.</p>@if(session('status'))<div class="login-alert login-alert--success">{{ session('status') }}</div>@endif
@if($errors->any())<div class="login-alert login-alert--error">@foreach($errors->all() as $error)<p>{{ $error }}</p>@endforeach</div>@endif
<form method="POST" action="{{ route('verificacion.confirmar') }}" class="mt-8 space-y-5">@csrf<div><label for="codigo" class="label-field">Código de verificación</label><input type="text" name="codigo" id="codigo" class="input-field !rounded-xl text-center text-lg tracking-[.45em]" inputmode="numeric" maxlength="6" autocomplete="one-time-code" required autofocus></div><button type="submit" class="btn-primary login-submit">Confirmar correo</button></form><form method="POST" action="{{ route('verificacion.reenviar') }}" class="mt-4 text-center">@csrf<button type="submit" class="text-sm font-semibold text-primary-600 hover:text-primary-700">Reenviar código</button></form></div></main></div>
@endsection
